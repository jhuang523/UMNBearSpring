#will contain recharge, initial conditions, boundary conditions 

network_dir: 'networks/branching_straight_network'
output_dir: 'output/branching_straight_network_gauss'
inflow_type : 'timeseries'  # 'constant', 'timeseries', 'ramp'
inflow_file : 'input_data/branching_straight_network_gauss/inflow_boundary.pkl'
head_boundary_type : 'constant'  # 'constant', 'timeseries', 'ramp'
head_boundary_file:  'input_data/branching_straight_network_gauss/head_boundary.pkl' #file with dict containing {(node,) : head_val}
initial_conditions_file : 'input_data/init_conditions/branching_straight_network_IC.pkl'  # file containing initial flowrate and water depth info
steady_state: False 
dt_max: 3600
t_max: 2160000  # fix so that if None, it tries the max timeseries or a default value

