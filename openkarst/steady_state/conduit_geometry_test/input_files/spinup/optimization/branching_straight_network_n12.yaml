#will contain recharge, initial conditions, boundary conditions 

network_dir: 'networks/branching_straight_network/n_12'
output_dir: 'output/branching_straight_network_n_12_ss'
inflow_type : 'timeseries'  # 'constant', 'timeseries', 'ramp'
inflow_file : 'input_data/inflow_boundary/branching_straight_network_n_12_ss.pkl'
head_boundary_type : 'constant'  # 'constant', 'timeseries', 'ramp'
head_boundary_file:  'input_data/head_boundary/branching_straight_network_n_12.pkl' #file with dict containing {(node,) : head_val}
initial_conditions_file : None  # file containing initial flowrate and water depth info
steady_state: False 
dt_max: 3600
t_max: 86400  # fix so that if None, it tries the max timeseries or a default value

