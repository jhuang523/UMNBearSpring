#will contain recharge, initial conditions, boundary conditions 

network_dir: 'networks/straight_conduit'
output_dir: 'output/straight_conduit_point_high_eta'
inflow_type : 'timeseries'  # 'constant', 'timeseries', 'ramp'
inflow_file : 'input_data/inflow_boundary/high_eta/straight_conduit_point_only.pkl'
head_boundary_type : 'constant'  # 'constant', 'timeseries', 'ramp'
head_boundary_file:  'input_data/head_boundary/straight_conduit.pkl' #file with dict containing {(node,) : head_val}
initial_conditions_file : 'input_data/init_conditions/straight_conduit_IC.pkl'  # file containing initial flowrate and water depth info
steady_state: False 
dt_max: 3600
t_max: 2160000  # fix so that if None, it tries the max timeseries or a default value

