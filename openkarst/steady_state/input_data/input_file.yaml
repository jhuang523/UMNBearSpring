#will contain recharge, initial conditions, boundary conditions 

network_dir: 'pykasso_n58'
output_dir: 'script_test'
inflow_type : 'timeseries'  # 'constant', 'timeseries', 'ramp'
inflow_file : 'inflow_boundary.pkl'
head_boundary_type : 'constant'  # 'constant', 'timeseries', 'ramp'
head_boundary_file:  'head_boundary.pkl' #file with dict containing {(node,) : head_val}
initial_conditions_file : 'initial_conditions.pkl'  # file containing initial flowrate and water depth info
steady_state: False 
dt_max: 1000
t_max: 86400  # fix so that if None, it tries the max timeseries or a default value

